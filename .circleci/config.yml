version: 2
jobs:
   build:
     docker:
       - image: microsoft/aspnetcore-build:2.0
     steps:
       - checkout
       - setup_remote_docker
       - run: |
          TAG=0.1.$CIRCLE_BUILD_NUM
          docker build -t aspcoreapitest:$TAG .